# DeepNetwork
The deep network analyzes the similarity measure between question and answers and categorizes the answer depending upon it's relevancy. It is my active research area, so will release the dataset after a period of time. 
The provided code runs with backend as tensorflow-1.2 version and uses keras-2.0.4 version.
The baseline model reaches the accuracy of 70%.
